# party/.htaccess

Options -Indexes

# Block direct access to config and schema files
<FilesMatch "^(config|schema)\.php$">
    Require all denied
</FilesMatch>

# Block JSON data files
<Files "*.json">
    Require all denied
</Files>

AddDefaultCharset UTF-8
