# admin/.htaccess — Only index.php and moderate.php should be reachable.

Options -Indexes

# Allow only these two entry points; deny everything else
<FilesMatch "^(?!index\.php$|moderate\.php$|thumb\.php$|poll\.php$|download_gallery\.php$)">
    Require all denied
</FilesMatch>
